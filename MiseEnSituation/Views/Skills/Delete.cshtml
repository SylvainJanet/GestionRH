@model MiseEnSituation.Models.Skill

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Skill</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Courses)
        </dt>
        <dd>
            @Html.DropDownListFor(model => model.Courses, new SelectList(Model.Courses.Select(o => o.Name), null, Model.Courses.Select(o => o.Name)), htmlAttributes: new { @class = "form-control" })
        </dd>
            
            <dt>
            @Html.DisplayNameFor(model => model.Posts)
        </dt>
        <dd>
            @Html.DropDownListFor(model => model.Posts, new SelectList(Model.Posts.Select(o => o.Description), null, Model.Posts.Select(o => o.Description)), htmlAttributes: new { @class = "form-control" })
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Employees)
        </dt>
        <dd>
            @Html.DropDownListFor(model => model.Employees, new SelectList(Model.Employees.Select(o => o.Name), null, Model.Employees.Select(o => o.Name)), htmlAttributes: new { @class = "form-control" })
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
